// import { combineReducers } from 'redux';

const uiReducer = (state = {}, action) => {
  Object.freeze(state);
  switch (action.type) {
    // case()
    default: 
      return state;
  }
}
export default uiReducer;